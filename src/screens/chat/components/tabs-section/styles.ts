import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  tabBar: {
    alignSelf: 'center'
  },
  indicator: {
    width: '70%',
    height: 3
  },
  listContainer: {
    backgroundColor: '#fff'
  }
})

export default styles
