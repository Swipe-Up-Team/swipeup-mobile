import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  container: {
    backgroundColor: '#fff'
  },
  list: {
    height: '100%',
    backgroundColor: '#fff'
  }
})

export default styles
