import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  container: {
    backgroundColor: '#fff'
  },
  list: {
    height: '100%',
    backgroundColor: '#fff'
  },
  input: {
    width: '100%',
    marginVertical: 16,
    borderWidth: 0
  }
})

export default styles
