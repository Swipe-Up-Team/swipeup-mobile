import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  container: {
    width: '100%',
    flexDirection: 'row',
    backgroundColor: '#fff',
    marginTop: 4,
  },
  avatar: {
    marginLeft: 10,
    marginRight: 6,
  },
  bannerContainer: {
    alignItems: 'center',
    flex: 1
  },
  banner: {
    height: '100%'
  },
})

export default styles
